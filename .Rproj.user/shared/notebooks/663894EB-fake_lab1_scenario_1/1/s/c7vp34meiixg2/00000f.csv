"0","jags_results <- jags_results |>
  pivot_longer(cols = everything()) |>
  mutate(value = round(value, digits = 2)) |>
  group_by(name, value) |>
  summarize(
    counts = n()
  )
"
"2","`summarise()` has grouped output by 'name'. You can override using the `.groups` argument."
